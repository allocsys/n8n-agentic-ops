{
  "name": "n8n-crm-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev -c ../wrangler.toml",
    "deploy": "wrangler deploy -c ../wrangler.toml",
    "test": "vitest run",
    "test:watch": "vitest",
    "db:create": "wrangler d1 create n8n-crm-worker-db -c ../wrangler.toml",
    "db:migrate:local": "wrangler d1 execute n8n-crm-worker-db --file=./schema.sql -c ../wrangler.toml",
    "db:migrate:remote": "wrangler d1 execute n8n-crm-worker-db --file=./schema.sql --remote -c ../wrangler.toml"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.6",
    "vitest": "^4.1.0",
    "wrangler": "^4.113.0"
  }
}
